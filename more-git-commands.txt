**1. What command is used to stage changes for commit in Git?**
   - a. git add

**2. Which Git command initializes a new repository?**
   - a. git init

**3. What does `git status` show?**
   - d. Branch information

**4. How do you create a commit in Git?**
   - a. git add

**5. What command is used to view the commit history in Git?**
   - a. git log

**6. Which Git command is used to clone a repository from a remote URL?**
   - c. git clone

**7. What does `git clone` do?**
   - b. Clone a remote repository

**8. How do you add a remote repository to your local Git repository?**
   - a. git init

**9. What is the purpose of `git pull`?**
   - b. Fetch changes from a remote repository

**10. How do you push changes to a remote repository in Git?**
   - c. git pull

**11. Which Git command is used to see the changes made in the working directory?**
   - b. git diff

**12. What is the purpose of `git push`?**
   - a. Fetch changes from a remote repository

**13. What does `git log` display?**
   - a. Staged changes

**14. How do you initialize a new Git repository on your local machine?**
   - b. git init

**15. What is the purpose of `git status`?**
   - b. Show changes made in the working directory

**16. Which command is used to fetch changes from a remote repository in Git?**
   - b. git fetch

**17. How do you create a new branch in Git?**
   - a. git branch

**18. What is the purpose of `git checkout`?**
   - a. Switch to a different branch

**19. How do you pull changes from a remote repository and merge them into your local branch?**
   - a. git fetch

**20. What does `git fetch` do?**
   - b. Pull changes from a remote repository
